document.addEventListener("DOMContentLoaded", function(event) {
    console.log('start clearing',location.host)
    // localStorage.clear();
    sessionStorage.clear();
    console.log('end clearing',location.host)
});
